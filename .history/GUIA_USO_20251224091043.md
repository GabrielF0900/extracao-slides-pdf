# 📘 Guia de Uso - Extração de Slides para PDF

## 🚀 Preparação Inicial

### 1️⃣ **Instale as Dependências**

```powershell
pip install -r requirements.txt
```

Ou instale manualmente:
```powershell
pip install pyautogui keyboard img2pdf pygetwindow Pillow
```

### 2️⃣ **Configure o Corte (Crop) - IMPORTANTE!**

Abra o arquivo `extrair.py` e ajuste estas variáveis no topo de acordo com seu monitor:

```python
CORTE_TOPO = 160    # Aumentar: remove mais abas/barra de endereço
CORTE_BAIXO = 20    # Aumentar: remove mais barra de status
CORTE_LADOS = 10    # Aumentar: remove mais bordas laterais
```

> **Teste rápido:** Tire 1 print de teste. Se cortar o conteúdo, ajuste os valores.

## 🎬 Como Usar

### Passo 1: Execute o Script
```powershell
python extrair.py
```

### Passo 2: Digite o Nome do Arquivo
O script pedirá um nome. Digite sem extensão:
```
Nome do arquivo (sem extensão): Aula_Historia_Slide_01
```

### Passo 3: Ative a Janela do Navegador
- O script **contará 3 segundos**
- **Clique RÁPIDO** na janela do navegador para ativá-la
- O script capturará essa janela

### Passo 4: Capture os Slides
- **ENTER:** Captura o slide atual e passa para o próximo
- **ESC:** Encerra a captura e gera o PDF

## 📋 O que o Script Faz

✅ **Captura automática de slides:**
- Detecta a janela ativa (navegador)
- Remove bordas, abas e menus automaticamente
- Salva cada frame como imagem PNG

✅ **Converte para PDF:**
- Agrupa todas as imagens em ordem
- Mantém qualidade original
- Nomeia o arquivo com sanitização automática (remove caracteres inválidos)

✅ **Backup automático:**
- Salva imagens em pasta temporária
- Se houver erro, não perde as imagens capturadas

## 📁 Saída

O PDF será salvo na mesma pasta do script:
```
Aula_Historia_Slide_01.pdf
```

> **Nota:** Caracteres especiais (/, \, :, ?, ") são removidos automaticamente do nome.

## 💡 Dicas Importantes

1. ✅ **Teste primeiro com 2-3 slides** para calibrar o corte
2. ✅ **Use nomes descritivos** para os PDFs (ex: `Python_Aula_02`)
3. ✅ **Deixe a janela cheia** (fullscreen se possível) para melhor resultado
4. ✅ **Feche notificações/popups** do navegador antes de começar
5. ✅ **Rápido:** Use `ENTER` e setas para navegar nos slides rapidamente

## 🔧 Personalizações (Opcional)

**Aumentar a qualidade das imagens capturadas:**

Procure pela linha com `scales=` e aumente o número:
```python
# Padrão
screenshot = pyautogui.screenshot(region=region)

# Para melhor qualidade em monitores 4K, ajuste conforme necessário
```

**Mudando o tipo de arquivo:**

Troque `.pdf` por `.jpg` para gerar uma pasta de imagens em vez de PDF.

## ❌ Resolvendo Problemas

| Problema | Solução |
|----------|---------|
| **PDF vazio ou preto** | Aumente `CORTE_TOPO`. O script pode estar capturando acima da janela. |
| **Corta o conteúdo das bordas** | Aumente `CORTE_LADOS` |
| **Aparece a barra de endereço** | Diminua `CORTE_TOPO` |
| **Captura a janela errada** | Clique mais rápido na janela do navegador durante a contagem |
| **"ModuleNotFoundError"** | Rode `pip install -r requirements.txt` novamente |
| **Imagens muito pequenas** | Use monitor em fullscreen ou aumente a resolução |

## 🎯 Workflow Recomendado

1. Abra o navegador com o slide que quer extrair
2. Execute `python extrair.py`
3. Digite o nome (ex: `Aula_02_Parte_01`)
4. Clique na janela do navegador
5. Pressione `ENTER` para cada slide
6. Pressione `ESC` quando terminar
7. Seu PDF está pronto! 🎉

## 📞 Suporte

Se tiver problemas, verifique:
- ✅ Python 3.8+ instalado: `python --version`
- ✅ Dependências instaladas: `pip list | findstr pyautogui`
- ✅ Permissões do Windows (RPA pode precisar de admin)
